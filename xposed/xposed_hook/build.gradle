apply from: "$rootDir/config/app.gradle"

android {
    defaultConfig {
        applicationId "com.pds.xposed.hook"
    }
}

dependencies {
    implementation deps.ax.appcompat
    implementation deps.ui.constraintlayout
    compileOnly files('libs/XposedBridge-82.jar')
}
