apply from: "../../config/module.gradle"
apply from: "../../config/kotlin.gradle"

dependencies {
    implementation deps.kt.kotlinJdk
    implementation deps.data.gson
    api deps.data.glide
    annotationProcessor deps.data.glideCompiler
    // 里面使用了protobuf,如果项目中使用自己的protobuf版本，编译会报错
    implementation deps.data.tinkAndroid
}

