apply from: "../../config/module.gradle"
apply from: "../../config/hilt.gradle"

apply plugin: 'dagger.hilt.android.plugin'
dependencies {
    api deps.ax.multidex
    implementation deps.ax.appcompat
    implementation project(path : ":module:SampleModule")
    implementation project(path : ":base:lib_res")
    // 每个module都要配置的，为了应对单个module 分别编译的场景
    annotationProcessor deps.alibaba.arouterCompiler
}